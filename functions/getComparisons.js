/*
const Comparison = require("../classes/Сomparison");
const getCalculatedQuantities = require("./getCalculatedQuantities");

function getComparisons(inputQuantities) {
    const calculatedQuantities = getCalculatedQuantities(inputQuantities);
    const comparisons = {
        formula_5: new Comparison("N / A_n ≤ R_y * γ_c", inputQuantities, calculatedQuantities),
        formula_7: new Comparison("N / ( φ * A ) ≤ R_y * γ_c", inputQuantities, calculatedQuantities),
    };

    return comparisons;
}

module.exports = getComparisons;*/
